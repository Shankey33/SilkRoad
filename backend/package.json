{
  "type": "module",
  "engines": {
    "node": "^18 || ^20 || >=22"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.1",
    "nodemon": "^3.1.10"
  },
  "scripts": {
    "build": "cd ../frontend && npm install --include=dev && npm run build",
    "start": "node app.js"
  }
}
